<Window x:Class="YouTubeClient.Client.Views.VideoDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ViewModels="clr-namespace:YouTubeClient.Client.ViewModels"
        mc:Ignorable="d"
        Title="Video Title" Height="450" Width="800"
        d:DataContext="{d:DesignInstance {x:Type ViewModels:VideoDetailsViewModel}}">
    <Grid>
        <DataGrid Margin="10,10,10,10" ItemsSource="{Binding VideoDetailsOutput}" AutoGenerateColumns="False" CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding VideoName}" />
                <DataGridTextColumn Header="Url" Binding="{Binding VideoUrl}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
